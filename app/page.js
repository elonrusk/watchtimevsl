export default function Page() {
  return (
    <main style={{ padding: 20 }}>
      <h1 style={{ margin: 0 }}>VSL Watchtime — OK</h1>
      <p style={{ opacity: 0.75 }}>
        Utilise /api/yt-watch (POST) et /api/yt-watch/stats (GET), ou va sur /dashboard.
      </p>
    </main>
  );
}
